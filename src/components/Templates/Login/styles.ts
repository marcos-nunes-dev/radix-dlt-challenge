import styled from 'styled-components';
import { Box } from 'components/Box';

export const ArtboardWrapper = styled(Box)`
    background: url('/img/video-preview.jpg') no-repeat center center / cover;
`;
